spring:
  profiles:
    activate: dev

  jpa:
    show-sql: true
    generate-ddl: true
    hibernate:
      ddl-auto: create
    output:
      ansi:
        enabled: always

    logging:
      level:
        org.springframework.web: DEBUG
        hexlet.code: TRACE